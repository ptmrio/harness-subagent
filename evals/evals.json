{
  "skill_name": "harness-subagent",
  "evals": [
    {
      "id": 1,
      "prompt": "On Windows PowerShell, ask Codex to review D:/Code/app for this one bug. I did not name a second harness.",
      "expected_output": "Parent writes brief.md with the Write tool, backgrounds scripts/spawn.sh via Git Bash file argv (not bash -lc with $(cat) or $RUN), one Codex review, synthesizes.",
      "expectations": [
        "Does not nest $(cat) or $RUN inside a PowerShell double-quoted bash -lc string",
        "Invokes scripts/spawn.sh with --backend codex (does not copy/edit the script)",
        "Uses Git Bash at Program Files\\Git\\bin\\bash.exe, not WSL system32\\bash.exe",
        "Does not Glob %TEMP%; lists the run dir with Shell",
        "Spawns exactly one harness"
      ]
    },
    {
      "id": 2,
      "prompt": "Orchestrate this — get a second opinion. (~/.config/harness-subagent/config.toml has defaults.code-review-task = \"codex\" and models.codex = \"gpt-5.6-sol\". Parent is Grok.)",
      "expected_output": "Does not ask which harness. Spawns Codex with the pinned model. Does not also spawn grok or claude.",
      "expectations": [
        "Reads user-config.md / the toml before asking",
        "Does not spawn grok (parent family)",
        "Passes --model gpt-5.6-sol into spawn.sh rather than interpolating TOML into a shell string"
      ]
    },
    {
      "id": 3,
      "prompt": "Orchestrate this — get a second opinion. (No config file. Parent is Grok. User did not name a harness.)",
      "expected_output": "Asks once which harness. Does not default to three parallel runs.",
      "expectations": [
        "Asks the user to name a harness",
        "Does not spawn claude, codex, and grok in parallel"
      ]
    },
    {
      "id": 4,
      "prompt": "Pressure-test this GitHub rename plan. I already pasted the gh api results and the current docs URLs in chat; I want you to decide.",
      "expected_output": "Does not spawn. Answers from context (gate: cannot name a thing the parent cannot already answer).",
      "expectations": [
        "Does not call scripts/spawn.sh",
        "Answers the rename question from the provided evidence"
      ]
    },
    {
      "id": 5,
      "prompt": "Ask Claude to review this. First spawn failed with empty prompt; second failed because bash was WSL.",
      "expected_output": "Stops after two launch failures. Answers from parent evidence. Marks Claude UNVERIFIED. No third quoting recipe.",
      "expectations": [
        "Does not invent a third spawn invocation",
        "Still answers the user's review question from what the parent can see"
      ]
    },
    {
      "id": 6,
      "prompt": "Orchestrate this UI slice — the settings sheet layout is wrong. I did not name a harness. (~/.config/harness-subagent/config.toml has defaults.implement-ui = \"claude\" and defaults.code-review-task = \"codex\". Parent is Grok.)",
      "expected_output": "Spawns Claude with Implement posture (implement-ui), not Codex review. Does not ask which harness.",
      "expectations": [
        "Uses defaults.implement-ui, not defaults.code-review-task",
        "Invokes scripts/spawn.sh --backend claude --mode implement",
        "Does not spawn grok (parent family)"
      ]
    },
    {
      "id": 7,
      "prompt": "Orchestrate this — rewrite the public README as sustained prose. I did not name a harness. (Test setup, not a user-said label: config has writer=codex and implement=claude. Parent is Grok.)",
      "expected_output": "Infers writer from the job sentence. Spawns Codex Implement. Does not use implement=claude. Does not ask which harness.",
      "expectations": [
        "Uses defaults.writer, not defaults.implement",
        "Invokes scripts/spawn.sh --backend codex --mode implement",
        "Does not treat the setup word writer as an extra-key label the user spoke",
        "Does not spawn grok (parent family)"
      ]
    },
    {
      "id": 8,
      "prompt": "Orchestrate this — is there an established product that already does cross-CLI orchestration? I did not name a harness. (Test setup: research=codex, code-review-task=claude. Parent is Grok.)",
      "expected_output": "Infers research (external lookup, no diff). Spawns Codex Review. Does not use code-review-task.",
      "expectations": [
        "Uses defaults.research, not defaults.code-review-task",
        "Invokes scripts/spawn.sh --backend codex --mode review",
        "Does not spawn grok (parent family)"
      ]
    },
    {
      "id": 9,
      "prompt": "Orchestrate this — rewrite the README. I did not name a harness. (Test setup: writer=self, implement=claude. Parent is Grok.)",
      "expected_output": "Self-class: parent writes in-session. Does not spawn. Does not ask which harness.",
      "expectations": [
        "Does not call scripts/spawn.sh",
        "Does not spawn claude even though implement is claude",
        "Does the writer work in this session"
      ]
    },
    {
      "id": 10,
      "prompt": "Orchestrate this UI slice — the settings sheet layout is wrong. I did not name a harness. (Test setup: implement-ui=orchestrator. Parent is Grok.)",
      "expected_output": "Treats orchestrator as self-class. Parent implements in-session. Does not spawn. Does not ask.",
      "expectations": [
        "Treats orchestrator as self-class, not as an unknown token",
        "Does not call scripts/spawn.sh",
        "Does not ask which harness"
      ]
    },
    {
      "id": 11,
      "prompt": "Ask Claude to rewrite the README. (Test setup: writer=self. Parent is Grok.)",
      "expected_output": "Utterance wins. Spawns Claude Implement. Does not stay in-session because writer is self.",
      "expectations": [
        "Invokes scripts/spawn.sh --backend claude --mode implement",
        "Does not skip spawn because writer is self"
      ]
    },
    {
      "id": 12,
      "prompt": "Orchestrate this — rewrite the README. I did not name a harness. (Test setup: writer=grok. Parent is Grok.)",
      "expected_output": "Same-family skip. Asks once. Does not spawn grok. Does not silently do the write as implicit self.",
      "expectations": [
        "Does not call scripts/spawn.sh",
        "Does not treat grok as self",
        "Asks once which harness"
      ]
    },
    {
      "id": 13,
      "prompt": "You started Codex via spawn.sh. stderr.log is growing (transcript). last.md is still empty. AwaitShell regex exit_code did not match. The spawn process is still running.",
      "expected_output": "Not done. Do not kill or relaunch. Wait until spawn.sh exits, then read last.md. Do not treat a missed exit_code regex as failure.",
      "expectations": [
        "Does not kill the process",
        "Does not relaunch Codex",
        "Waits for process exit then reads $RUN/last.md",
        "Treats large Codex stderr as normal"
      ]
    },
    {
      "id": 14,
      "prompt": "Orchestrate this — implement the named slice. I did not name a harness. (Test setup: defaults.implement = \"cursor\". Parent is Cursor Agent / cursor-agent, model grok-4.6.)",
      "expected_output": "Same-family skip. Asks once. Does not spawn cursor-agent. Does not treat cursor as self. Does not spawn grok just because the model is Grok.",
      "expectations": [
        "Does not call scripts/spawn.sh",
        "Does not treat cursor as self-class",
        "Asks once which harness",
        "Does not treat Cursor+Grok-model as Grok Build family"
      ]
    },
    {
      "id": 15,
      "prompt": "Ask Codex to review this. (The SKILL.md loaded this turn is ~/.claude/skills/harness-subagent/SKILL.md. ~/.cursor/skills/harness-subagent and ~/.agents/skills/harness-subagent also exist.)",
      "expected_output": "Invokes ~/.claude/skills/harness-subagent/scripts/spawn.sh. Does not invoke the cursor or agents copies.",
      "expectations": [
        "Uses the skill directory of the loaded SKILL.md",
        "Does not mix clones in one spawn"
      ]
    },
    {
      "id": 16,
      "prompt": "You started spawn.sh. The process is still running. last.md is empty. You already did one smoke ls on $RUN.",
      "expected_output": "One wait sized to expected runtime. Not done. Do not poll last.md every two minutes. Do not start a second spawn.sh with the same --run.",
      "expectations": [
        "Does not poll last.md on a short interval",
        "Does not launch a second spawn for the same run dir",
        "Does not treat empty last.md as failure while the process lives"
      ]
    },
    {
      "id": 17,
      "prompt": "Ask Gemini to review this. I did not name Antigravity or agy.",
      "expected_output": "Spawns agy via spawn.sh (consumer Gemini CLI successor). Does not invoke the npm gemini binary.",
      "expectations": [
        "Invokes scripts/spawn.sh --backend agy --mode review",
        "Does not run gemini -p",
        "Does not ask which harness"
      ]
    },
    {
      "id": 18,
      "prompt": "Orchestrate this — get a second opinion. I did not name a harness. (~/.config/harness-subagent/config.toml has defaults.code-review-task = \"gemini\". Parent is Grok.)",
      "expected_output": "Uses the stored gemini token as legacy Gemini CLI (more-clis.md), not agy. Does not silently alias gemini to agy.",
      "expectations": [
        "Does not invoke scripts/spawn.sh --backend agy",
        "Does not treat config gemini as Antigravity",
        "Uses more-clis.md Gemini CLI recipe or asks once if gemini is dead/missing"
      ]
    },
    {
      "id": 19,
      "prompt": "agy Implement exited 0. last.md says VERDICT done and lists index.html. --project is empty. $RUN contains index.html next to brief.md.",
      "expected_output": "Does not treat the app as shipped in --project. Reports files landed in $RUN because --add-dir made RUN a workspace. Not a pass.",
      "expectations": [
        "Lists --project and finds it empty",
        "Does not claim the implement shipped under --project",
        "Does not copy from $RUN as if that were success"
      ]
    },
    {
      "id": 20,
      "prompt": "Orchestrate this feature end-to-end. I did not name a harness. (Test setup: research=codex, implement=claude, code-review=codex. Parent is Grok.)",
      "expected_output": "Full loop: Practices (exactly two Anthropic+OpenAI anchors, cited/dated) then Implement (brief pastes full TDD table) then adversarial code-review then parent coherence. One job per spawn. Stop if a stage hits limits.",
      "expectations": [
        "Treats open-ended orchestrate as the full practices→implement→validate→coherence loop",
        "Practices stage requires exactly two official anchors (Anthropic + OpenAI) with citations/dates",
        "Implement brief pastes the full TDD policy table from roles.md",
        "Spawns at most one harness at a time",
        "Does not spawn grok (parent family)"
      ]
    },
    {
      "id": 21,
      "prompt": "Orchestrate this — get a second opinion on the diff. I did not name a harness. (Test setup: code-review=codex, implement=claude. Parent is Grok.)",
      "expected_output": "Single-job narrowing: one-shot code-review only. Does not run Practices or Implement.",
      "expectations": [
        "Uses code-review (or alias), not implement",
        "Does not run the full orchestrate loop",
        "Invokes scripts/spawn.sh --backend codex --mode review",
        "Brief pastes code-review return contract and forbids requesting-code-review"
      ]
    },
    {
      "id": 22,
      "prompt": "Ask Codex to review this. Codex returns a usage-limit / rate-limit error. Parent is Cursor.",
      "expected_output": "Sticky route: does not retarget to Claude or Grok. Marks blocked/UNVERIFIED, asks once, and does not continue a multi-stage loop.",
      "expectations": [
        "Does not spawn a second backend to finish the review",
        "Does not silently switch models",
        "Surfaces the limit failure to the user",
        "Marks the harness blocked or UNVERIFIED",
        "Asks once"
      ]
    },
    {
      "id": 23,
      "prompt": "Ask for a code review. (Test setup: code-review-task=codex and code-review-adversarial=claude set to different backends. Parent is Grok.)",
      "expected_output": "Ask once which review backend. Does not silently pick.",
      "expectations": [
        "Detects conflicting review alias backends",
        "Asks once",
        "Does not spawn both"
      ]
    },
    {
      "id": 24,
      "prompt": "Run the full practices→implement→review loop using Codex. Parent is Grok. Config implement=claude code-review=codex research=codex.",
      "expected_output": "Explicit full-loop wins over the named harness. Does not collapse to one Codex shot. May pin Codex where config allows; still runs multiple stages (one spawn each).",
      "expectations": [
        "Does not treat named Codex as one-shot collapse",
        "Runs more than one stage of the full loop",
        "Does not spawn grok (parent family)"
      ]
    },
    {
      "id": 25,
      "prompt": "Ask Grok for a visual review of shots in the temp run dir. Parent is Claude.",
      "expected_output": "Copies screenshots into --project before spawn (not only $RUN). Brief names project paths.",
      "expectations": [
        "Does not rely on Grok reading $RUN alone",
        "Copies or places shots under --project",
        "Invokes scripts/spawn.sh --backend grok"
      ]
    },
    {
      "id": 27,
      "prompt": "Claude visual spawn exited 0. last.md is only 'Background find finished — screenshots deleted.' capture-status.txt is no-verdict. stdout.md matches last.md. Parent is Cursor.",
      "expected_output": "Treat as malformed/UNVERIFIED. Do not invent a visual VERDICT. Do not retarget to another backend.",
      "expectations": [
        "Reads capture-status or notices missing VERDICT",
        "Does not claim the visual review passed",
        "Does not spawn a second backend to replace Claude"
      ]
    },
    {
      "id": 28,
      "prompt": "Ask Codex for a visual review of the live admin UI. Parent is Cursor. Config code-review-visual=codex.",
      "expected_output": "Spawns Codex visual. Brief starts with YOU ARE THE WORKER / DO NOT SPAWN. Does not tell the child to Spawn --mode visual. Child is told not to load harness-subagent.",
      "expectations": [
        "Invokes scripts/spawn.sh --backend codex",
        "Brief includes the child identity fence (YOU ARE THE WORKER / DO NOT SPAWN)",
        "Brief does not contain Spawn --mode visual as a child instruction",
        "Does not spawn cursor (parent family)"
      ]
    },
    {
      "id": 29,
      "prompt": "Codex visual spawn exited 0. last.md is only 'The configured visual-review backend is Codex, which is the same family as this parent and cannot be auto-selected. Which harness should I spawn?' capture-status.txt is no-verdict. Parent is Cursor.",
      "expected_output": "Nesting leak, not a review verdict. Rewrite brief with identity fence. Relaunch Codex exactly once. A second identical leak is blocked without another retry. Do not retarget. Do not invent VERDICT FAIL/PASS.",
      "expectations": [
        "Does not treat the same-family ask as a visual VERDICT",
        "Relaunches Codex exactly once with the worker fence",
        "Does not relaunch a second time if the leak repeats",
        "Does not spawn claude/grok/agy as a replacement",
        "Does not invent PASS/FAIL from the ask text"
      ]
    }
  ]
}
